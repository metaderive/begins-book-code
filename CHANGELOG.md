# Changelog

本ライブラリの変更履歴。バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [0.1.0-beta.2] - 2026-09-11

### 修正
- **ヘッダー先頭ビットの符号化を実機一致に修正**。40枚ブックの一部（多重コピーのカードが
  特定のページ配置になる構成、例: ×3のカードが page1 のみ／page0 のみ）で **`encodeBook` が
  実機と違うバイトを出力**し、生成コードが実機で取り込めず・`decode` でも枚数がズレていた
  不具合を修正した。
  - ヘッダー各ページ先頭バイト＝`[先頭ビット][H=(×3 or ×4 あり)][×1あり][×2種類数:5bit]`。
    H が立つと `n4(4bit)+n3(4bit)` をペアで書く。
  - **先頭ビット**: page0 は「×3のみ（×3あり・×4なし）で 0、それ以外 1」、page1 は常に 0
    （マーカー）。旧実装は page0 を定数1にしていたため ×4 の無いブックでズレ、decoder も
    先頭ビット0のコードを弾いていた。
  - decoder は page0 先頭ビットを構造判定に使わず読み飛ばし、`0x80` ガードを撤去。
- 実機コード 12 本（既知 10 ＋ 新規 2: ×3が page1のみ／page0のみ）で **encode バイト完全一致・
  decode 復元一致**を確認。妥当な全 40 枚ヒストグラム 257,954 通りの `encode↔decode` 往復も
  0 件失敗。

## [0.1.0-beta.1] - 2026-07-26

初回のβ公開。『カルドセプト ビギンズ』（Nintendo Switch 2 / 日本語版 Ver 1.0.3）の
ブック共有コードのデコード／エンコードを、公式仕様なしのリバースエンジニアリングで実装。

### 実装済み（解読完了）
- **文字レイヤー**: 記号置換 Base64（`I→&`, `O→?`, `0→#`, `l→@`）の相互変換
- **ヘッダー**: ページ別 × 枚数クラス別の種類数をビット詰めした可変長ヘッダーの文法
- **カードID方式**: 真ID＝下位1バイト＋ページ0/1（ID列の2部構成から位置で復元）
- **Aカードフィールド**: `(ページ, ページ内インデックス)` 参照によるエース指定のデコード／
  エンコード（ページ0/ページ1どちらの任意カードも指定でき、往復可能）。エースの並び順は
  実機と同様、ID列の出現順（ページ0→ページ1、各ページ内index順）へ正規化する
- **エンコーダ**: 常に**本ライブラリの既定順**（採用したカード一覧の basicSortNo 順。
  "正しい並び"ではなく決め打ちの一定順）で出力する。`encodeBook(decode(code))` を通すと
  登録順違いの同一デッキが同一コードに収束＝重複排除・同一判定に使える（※同じ順序規則を
  使うツール内でのみ成立。並べ方が違えばコードも変わる）
- **実機往復テスト合格**: 本ライブラリ生成コードを実機が正しく復元（枚数混在・Aカード付き含む）

### カードID表
- **収録カード全 398 種のカードIDを確定**（page0 254・page1 144。実機ロバストプローブ法で全種収穫）
- 未使用ID枠（歯抜け）に当たった場合のみデコードは `"?"` を返す

### 既知の制限（β）
- 真ID空間は512枚が上限（2ページ方式の構造的天井）
- **フォーマットはゲームのアップデートで無効化される可能性がある**

[0.1.0-beta.2]: https://github.com/metaderive/begins-book-code/releases/tag/v0.1.0-beta.2
[0.1.0-beta.1]: https://github.com/metaderive/begins-book-code/releases/tag/v0.1.0-beta.1
